Data Binding -Sharing data inside a component

1) One way Binding
    
html -> ts or ts -> html

    ts:-component
    html:-view

    1.1 Component to view (ts -> html)
        a) string interpolation - {{expression}}
        b) Property Binding - [attribute name]="Property"

    1.2 View to Component (html -> ts)
        a)Event Binding -(event name)="function name()" 
        b)Event Binding using $(event) - (event name)="function name($event)  
        c)Event Binding using template referencing variables-#variable name

2)Two Way Binding


Angular Routing

    -To set path for a component
    
    {
        path:'value',component:class name
    }

    .
    .
    .
    .

    Frontend and Backend Integration
    
      Import HTTPClientModule - for creating http request
      Inject HTTPClient
      Using HTTPClientModule - for making API requests

      **************

      (use of cors - 2 dffrnt ports)
      install cors - backend
      import cors
      path
      httprequest
      import in app module 
      http client module ...import..dependency injection(dataservice)

      *******************

      Frontend and Backend Integration

      import HTTPClientModule - for creating http request
      inject HTTPClient
      using HTTPClientModule - for making API requests

      ******************

      Asynchronous call

      RsJs - Reactive Extension for JavaScript
           - To solve asynchronous request events 
    
         1)Observable(alternative of promise)(promise=>then&catch)
         
             a)Resolve -subscribe()

             b)Reject -catch()